SELECT ls.LookupSetId,
       ls.SetName,
       ls.Label,
       ls.Description,
       ls.Container,
       ls.CreatedBy,
       ls.Created,
       ls.ModifiedBy,
       ls.Modified,
       ls.Lsid,
       ls.ObjectId
FROM snd.LookupSets ls